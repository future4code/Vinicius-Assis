

describe("Testes exercicio 4", ()=>{
//R= a) performAttack, pois ela se executada causara alterações nas propiedades dos lutadores.

    test("Criando Mocks, letra: b)", () => {
    const performAttackMock = jest.fn(() => {
			return true
		});
    });

    test("Criando Mocks, letra: c)", () => {
    const performAttackMock = jest.fn(() => {
			return false
		});
    });

})